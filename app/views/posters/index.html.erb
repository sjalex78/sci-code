<div class="w-full ">
<div>
  <div class="flex justify-between items-center">
    <h1 class="font-bold text-xl">Your Posters</h1>
    <%# <p>TO FIX:PLACE A LIST OF POSTER I AM A CREATOR</p> %>
    <%= link_to 'Create a new poster', new_poster_path, class: "rounded-lg py-3 px-5 bg-blue-600 text-white block font-medium" %>
  </div>
  <div class="w-full rounded-lg shadow-lg lg:w-1/3 mt-2">
    <ul class="divide-y divide-gray-100">
        <% @posters.each do |poster| %>
      <div class="p-3 hover:bg-orange-200 hover:rounded-lg hover:text-blue-200 p-2">
        <div id="<%= dom_id poster %>">
        <%= link_to poster.title, poster %>
          <i class="fa fa-eye"></i>
        </div>
      </div>
    </ul>
  <% end %>
  </div>
  <div class="mt-2 flex justify-between items-center">
  <h1 class="font-bold text-xl">Your Collaborations</h1>
    <p>TO FIX:PLACE A LIST OF POSTER I AM AN EDITOR</p>
  </div>
</div>
</div>
